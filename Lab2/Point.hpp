#include <iostream>

using namespace std;

/*class Wektor{
private:
    int x,y;
public:
    Wektor();
    Wektor(int x, int y);

    double getx(){
        return this -> x;
    }

    void setx(int x){
        x = this -> x;
    }

    double gety(){
        return this -> y;
    }

    void sety(int y){
        y = this -> y;
    }

    // strumienie
    friend ostream& operator <<(ostream& str, const Wektor &wektor);

    // wektor * liczba, liczb * wektor
    friend Wektor operator *(const Wektor &wektor , int liczba);
    friend Wektor operator *(int liczba, const Wektor &wektor);

    //wektor * macierz
    Wektor operator *(Matrix macierz);

    // zmiana znaku
    Wektor operator -();

    //porównanie
    bool operator ==(const Wektor &wektor) const;
    bool operator !=(const Wektor &wektor) const;

    //dostęp do pola
    double operator [](char i) const;

    //rzutowanie
    operator int();
};*/
